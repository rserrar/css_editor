# Protocol de comunicació

Aquest document queda substituït per `docs/06-communication.md`, que descriu el contracte real actual del protocol.

Motiu:

- aquí s'hi documentava un handshake antic amb `ready`
- el sistema real actual fa servir `hello`, `preview:info:request/response`, `config:request` i `config:replaceAll`

Consulta sempre:

- `docs/06-communication.md`
